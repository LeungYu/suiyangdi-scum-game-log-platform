export declare class AdminCommand {
    constructor(scumId: string, steamId: string, sessionId: string, content: string, sendTimeStamp: string, otherConfig?: any);
    id: number;
    scumId: string;
    steamId: string;
    sessionId: string;
    content: string;
    otherConfig: any;
    sendTimeStamp: string;
}
