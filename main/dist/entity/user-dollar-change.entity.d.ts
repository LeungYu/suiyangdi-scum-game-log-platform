export declare class UserDollarChange {
    constructor(userId: any, balance: number, reason?: string);
    id: number;
    userId: any;
    balance: number;
    reason: string;
    createdTimeStamp: string;
}
