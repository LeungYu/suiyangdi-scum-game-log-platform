export declare class ChatMessage {
    constructor(scumId: string, steamId: string, sessionId: string, type: string, content: string, sendTimeStamp: string);
    id: number;
    scumId: string;
    steamId: string;
    sessionId: string;
    type: string;
    content: string;
    sendTimeStamp: string;
}
