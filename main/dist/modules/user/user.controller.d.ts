import { Page } from '../../dto/page';
import { Response } from 'express';
import { SessionUser } from '../../dto/session-user';
import { AddUserDto, UpdateUserDto, UpdateUserPasswordDto, ListTopNUsersDollarDto, ListTopNUsersChargeDollarsDto, UserCheckInDto, UpdateUserLoginsDto } from '../../dto/user';
import { UserService } from './user.service';
import { UserPasswordService } from './user-password.service';
import { UserDollarService } from './user-dollar.service';
import { UserDollarChangeService } from "../user-dollar-change/user-dollar-change.service";
import { UserLocationsService } from "./user-locations.service";
import { ServerConfigService } from "../server-config/server-config.service";
import { AdminService } from "../admin/admin.service";
import { CheckUserLoginSysDto } from "../../dto/user-admin-sys";
import { UserLoginService } from "./user-login.service";
import { ItemService } from "../item/item.service";
import { LevelService } from "../level/level.service";
import { QueueService } from '../queue/queue.service';
export declare class UserController {
    private readonly userService;
    private readonly userLoginService;
    private readonly adminService;
    private readonly userPasswordService;
    private readonly userDollarService;
    private readonly userDollarChangeService;
    private readonly userLocationsService;
    private readonly serverConfigService;
    private readonly itemService;
    private readonly levelService;
    private readonly queueService;
    constructor(userService: UserService, userLoginService: UserLoginService, adminService: AdminService, userPasswordService: UserPasswordService, userDollarService: UserDollarService, userDollarChangeService: UserDollarChangeService, userLocationsService: UserLocationsService, serverConfigService: ServerConfigService, itemService: ItemService, levelService: LevelService, queueService: QueueService);
    info(userInfo: SessionUser, session: any, res: Response): Promise<any>;
    listTopNUserDollar(params: ListTopNUsersDollarDto, res: Response): Promise<Response<any, Record<string, any>>>;
    listTopNUserChargeDollars(params: ListTopNUsersChargeDollarsDto, res: Response): Promise<Response<any, Record<string, any>>>;
    infoUserLocation(userInfo: SessionUser, page: Page, res: Response): Promise<Response<any, Record<string, any>>>;
    checkUserLogin(userInfo: SessionUser, query: CheckUserLoginSysDto, res: Response): Promise<Response<any, Record<string, any>>>;
    add(body: AddUserDto, res: Response, session: any): Promise<Response<any, Record<string, any>>>;
    updateUser(userInfo: SessionUser, body: UpdateUserDto, res: Response): Promise<Response<any, Record<string, any>>>;
    updateUserPassword(userInfo: SessionUser, body: UpdateUserPasswordDto, res: Response): Promise<Response<any, Record<string, any>>>;
    userCheckIn(userInfo: SessionUser, body: UserCheckInDto, res: Response): Promise<Response<any, Record<string, any>>>;
    updateUserLogins(userInfo: SessionUser, body: UpdateUserLoginsDto, res: Response): Promise<Response<any, Record<string, any>>>;
}
